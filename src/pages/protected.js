import React from "react";
import auth from '../components/auth';

const Protected = () => {
    return (
        <div>
            <h1>Protected</h1>
        </div>
    )
}

export default Protected;