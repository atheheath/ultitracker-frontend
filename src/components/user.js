class User {
    constructor({
        username,
        email,
        fullName
    }) {
        this.username = username
        this.email = email
        this.fullName = fullName
    }
}

export default User;